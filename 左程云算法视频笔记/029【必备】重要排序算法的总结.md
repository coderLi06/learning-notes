```java
/*
只在乎不同值，稳定性没有意义，在乎不同实例时有意义

//冒泡排序具备稳定性（相邻两数交换，相同则不交换）(n^2)
//插入排序具备稳定性（新插入的数与相邻两数交换，相同则不交换）(n^2)
希尔本身是多组的插入排序，和普通插入相比改良了一下而已，不常用
//! 归并排序具备稳定性（合并时相同数先拷贝左边，不交换相对位置）(n log n)(n)
//计数排序与基数排序具备稳定性（计数排序在计数时，先出现的数先放入结果数组，基数排序是多次计数排序）(n + k)

//选择排序不具备稳定性（找最小的数与0位置数交换，原0位置数相对次序变化）(n^2)
//!!! 快速排序不具备稳定性（分区时相同数可能交换，原相对位置变化）(n log n)(log n)
随机快速排序复杂度要按照概率上的期望估计，按照最差复杂度估计毫无意义
//! 堆排序不具备稳定性（堆化时相同数可能交换，原相对位置变化）(n log n)

基于比较的排序，时间复杂度n logn 空间复杂度低于n，还具备稳定性的算法目前未找到
TimSort也不行，算法面试笔试比赛都很少用

数据量小的情况下，用插入排序代码简单
性能优异、实现简单且利于改进（面对不同业务可选择不同划分策略）、不在乎稳定性：随机快排
性能优异、不在乎额外空间占用、具有稳定性：归并排序
性能优异、额外空间占用要求O(1)、不在乎稳定性：堆排序
*/
```java
